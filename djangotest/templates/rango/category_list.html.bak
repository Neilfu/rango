<ul class="nav nav-pills nav-stacked">
    {% if cat_list %}
        {% for cat in cat_list %}
            <li class="active"><a href="/rango/category/{{ cat.url }}">{{ cat.name }}</a></li>
        {% endfor %}
    {% else %}
        <li>No categories at present.</li>
   {% endif %}
</ul>